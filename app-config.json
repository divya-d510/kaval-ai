{
  "command": "python -m uvicorn app.main:app --host 0.0.0.0 --port $X_ZOHO_CATALYST_LISTEN_PORT",
  "build_path": ".",
  "stack": "python_3_10",
  "env_variables": {
    "DATASTORE_BACKEND": "catalyst",
    "AUTH_ENABLED": "true"
  },
  "memory": 512
}
